#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void ciclo(int contador)
{
    int caso, valor;
    scanf("%i%i", &caso, &valor);
}

int main() 
{
    int contador;
    scanf("%i", &contador);
    ciclo(contador);
	return 0;
}